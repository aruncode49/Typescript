"use strict";
let myName = "arun";
let age = 22;
let isLoading = true;
let album; // string, number, boolean, etc.
let unionType; // union type
unionType = 12;
unionType = "string";
const sum = (a, b) => {
    return a + b;
};
// real use case of union type
let isActive; // 0,1 | true,false
let postId;
// reg exp
let regex = /\w+/g;
