"use strict";
const fruits = [];
const superHeros = [];
fruits.push("apple");
fruits.push("banana");
const allUsers = [
    {
        name: "abc",
        isAdmin: true,
    },
];
allUsers.push({
    name: "xyz",
    isAdmin: false,
});
// we can define type for two dimensional array also
let mlModel = [];
mlModel.push([1]);
mlModel.push([2]);
mlModel.push([3]);
